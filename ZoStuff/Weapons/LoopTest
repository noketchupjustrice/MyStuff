--most simple shit idk why you would ever need this
time = 1 -- you can edit the time lmao (set it to 0 for free robux 😱)
_G.LoopEquip = false
while _G.LoopEquip == true do -- while true fuck my ass
    black = {"Katana"}
    game.ReplicatedStorage.EquipWeapon:InvokeServer(unpack(black))
    wait(time)
    black = {"Naginata"}
    game.ReplicatedStorage.EquipWeapon:InvokeServer(unpack(black))
    wait(time)
    black = {"Kanabo"}
    game.ReplicatedStorage.EquipWeapon:InvokeServer(unpack(black))
    wait(time)
    hai = {"Tanto"}
    game.ReplicatedStorage.EquipWeapon:InvokeServer(unpack(hai))
end
